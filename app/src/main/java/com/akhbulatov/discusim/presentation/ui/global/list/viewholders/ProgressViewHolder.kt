package com.akhbulatov.discusim.presentation.ui.global.list.viewholders

import android.view.View

class ProgressViewHolder(itemView: View) : BaseViewHolder<Any>(itemView) {
    override fun bind(item: Any) {
        // При отображении элемента прогресса никакие данные не задаются
    }
}